import React from 'react';
import {FaPauseCicrcle, FaPlayCircle} from 'react-icons/fa'

const PlayPause = (isPlaing, activeSong, song, handlePause, handlePlay) => {
    return (
        <div>

        </div>
    );
};

export default PlayPause;